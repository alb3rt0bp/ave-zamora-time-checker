# Dependencias de test (no productivas). No se despliegan con la Lambda.
# Uso: pip install -r requirements-test.txt
boto3>=1.34,<2.0
moto[dynamodb,s3]>=5.0,<6.0
tox == 4.24.2
pluggy == 1.5.0
pytest == 8.3.5
coverage == 7.6.12
